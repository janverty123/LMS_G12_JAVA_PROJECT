# Currently unused — the Vite dev proxy (vite.config.ts) routes /api to the
# backend on localhost:8080, so no base URL is needed in development.
# Reserved for Phase 10 (production build) once the frontend is served
# separately from the backend and needs an absolute API origin.
VITE_API_BASE_URL=http://localhost:8080/api
